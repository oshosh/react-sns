
export const initialState = {
    post: {
        mainPosts: [],
    }
}

export default (state = initialState, action) => {
    switch (action.type) {
        case value:

            break;

        default:
            return state;
    }
}